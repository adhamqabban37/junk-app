# Build Progress: AI Salvage Yard Intelligence Platform

Tracks completion against `docs/BUILD_PLAN.md`. Check boxes as each phase's acceptance criteria are verified.

## Session 2026-08-24: catch-up — verified and pushed the two commits the previous session ran out of budget before documenting/pushing, started local dev.

Previous session (2026-08-23) shipped two more commits after this file's last entry below but hit its session limit before writing them up or pushing: `7cf8f61` (ARA/Car-Part.com A/B/C/X damage-unit grading for sheet-metal parts) and `8cf96ea` (manual/photo-less part grading + Car-Part.com-style taxonomy picker). Both were already committed locally (see their own commit messages above for full detail), just not documented here or pushed.

**This session:** pushed both to `origin/main` (was 1 commit behind, now in sync — `git push` succeeded, `7cf8f61..8cf96ea`). Re-verified the resulting `HEAD` is healthy before touching anything further: backend unit 30/30, frontend 209/209 (34 files), both workspaces lint clean (0 errors, only the pre-existing `no-unsafe-argument` warnings this doc already tracks as expected noise), `migrate` confirms the dev DB already has both new migrations (`1787510000000-AddAraPartGrading`, `1787520000000-AllowManualAiAnalysis`) applied — no pending migrations. `GEMINI_API_KEY` confirmed non-empty in `backend/.env`.

**Started local dev environment** for the user: Docker Desktop wasn't running (had to be launched manually — `C:\Users\vms\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`, not the default `Program Files` path this doc's own setup instructions assume), then `docker compose` confirmed Postgres+Redis already running. Backend (`npm run dev:backend`, port 3001) and frontend (`npm run dev:frontend`, port 3000) both confirmed serving `200` — route table logs cleanly including yesterday's new `/parts/:id/manual-grade` and `/parts/:id/merge` routes.

**Not done / open follow-ups (carried over, still true):**
- Live browser verification of the ARA grading + manual-grading UI has still never happened — same sandboxed-Chrome `localhost` limitation documented in the 2026-08-23 entry below.
- `docs/BUILD_PLAN.md` still doesn't reflect several sessions' worth of schema/flow changes (carried over from multiple earlier sessions).
- Car-Part.com/eBay integration still blocked on the user obtaining external access first (see the dedicated section further below) — no code-side action pending.

## Session 2026-08-23: all 5 phases of the photo -> part identity plan (`zesty-zooming-lemon.md`) now done. Read this first next time.

User asked to re-validate the phased plan and drive it to completion end-to-end without stopping for check-ins. Investigation found **Phases 3 and 4 were already fully implemented in the uncommitted working tree** (per-section Gemini calls, `group_id`-based grouping, one Part per confirmed group) but never documented in this file and never actually verified -- the last entry below still says "Phase 3 not started next." Only Phase 5 (the manager merge tool) was genuinely missing.

**Verifying Phases 3-4 surfaced a real, pre-existing data bug, not a code bug in the new work.** `part_taxonomies.name` was never actually unique -- every read site (`AiAnalysisService`'s `taxonomyIdByName` map, this suite's own e2e fixtures) has always assumed one row per name, but real duplicate-named rows had piled up in this dev database from repeated e2e runs and manual exploratory taxonomy creation ("Fender" existed 17 times; some duplicates were referenced by real Parts on the live BMW demo tenant, which is why `taxonomy.seed.ts`'s own `pruneSupersededTaxonomy()` couldn't fully collapse them). With duplicates present, `vehicle-analysis.e2e-spec.ts`'s own fixtures were reading back a different row than the one Gemini's suggestion actually resolved to -- 3 failing assertions that looked like logic bugs but were actually a data-integrity bug.

**Fixed with a real migration, not just a workaround:** `1787500000000-DeduplicatePartTaxonomyNames` merges every duplicate-name group onto one canonical row (repointing `parts`/`vehicle_photo_suggestions` FKs tenant-by-tenant to satisfy their `FORCE ROW LEVEL SECURITY` policies -- the migration connection is the same non-superuser app role as runtime, not exempt from RLS), then adds a real `UNIQUE(name)` constraint so this can't silently recur. `PartTaxonomy.name` entity column updated to match (`unique: true`). Verified live against the real BMW vehicle (`e22f5ef8-c919-4bac-9a1a-4c3b226d2461`): 706 taxonomy rows, all names now distinct.

That constraint then surfaced **9 e2e test files** that created a taxonomy row with a literal name colliding with either a real seeded name (`Alternator`, `Fender`, `Radiator`, `Water Pump`) or their own file's prior-run leftover (`Bumper`, `Headlight`, `Door` in `vehicle-photos.e2e-spec.ts`/`vehicles.e2e-spec.ts`) -- all fixed to reuse-by-name (`vehicle-analysis.e2e-spec.ts`'s new `getOrCreateExteriorTaxonomy()` helper, since that suite's fixtures must match real candidate-list names Gemini's mock returns) or a `${randomUUID()}`-suffixed unique name (everywhere else, where the literal name never matters to the test).

**Phase 5 -- shipped this session:** `PartsService.merge(tenantId, targetPartId, sourcePartIds)` folds every source Part's `PartImage`/`AiAnalysis` (plus `PricingHistory`/`Listing`/`Embedding`, since every part_id-bearing table has `ON DELETE CASCADE` from `parts` and anything not repointed before the delete is silently destroyed, not just orphaned) onto the target Part, then deletes the now-empty source Parts. Guards: rejects merging a part already `listed`/`sold`, rejects sources from a different vehicle, rejects `sourcePartIds` that reduce to nothing once the target itself is filtered out. Bumps an imageless manual target from `needs_manual_grading` to `pending_review` once a graded source lands on it (mirrors `analyzePartImage()`'s own status-bump rule). New `POST /parts/:id/merge` (MANAGER/OWNER, `MergePartsDto`), returns the merged part's fresh `detail()`. Frontend: Review Queue got a "Merge duplicates" toggle -- checkboxes per card, a bottom action bar disabled below 2 selections or when selections span more than one vehicle, target is deterministically the earliest-created selected Part (no manual target picker needed). New `mergeParts()` API client.

**Evidence:**
- New `backend/test/parts-merge.e2e-spec.ts` (8 tests: auth, target/source 404s, self-merge 400, cross-vehicle 400, already-listed 400, the real merge + aggregation flow, the manual-part status bump) -- 8/8.
- Full backend e2e suite: 101/102 (the 1 failure is the same pre-existing documented `app.e2e-spec.ts` ioredis teardown flake, unrelated -- confirmed by re-running it alone). Backend unit 20/20. Both workspaces lint (0 errors, pre-existing warnings only) and typecheck clean.
- Frontend: 4 new Review Queue tests (checkbox visibility, merge-button enablement, the merge-then-refetch flow, the cross-vehicle warning) + `mergeParts()` API client test -- full frontend suite 187/187 (32 files). Lint/typecheck clean.
- **Live-verified against the real BMW vehicle**, not just e2e mocks: minted a manager JWT for the real demo tenant (no password reset needed), started the real dev backend, and called `POST /parts/:id/merge` on two of the six real duplicate "Headlight Assembly" Parts the multi-part-per-photo fix created back in part 3. Confirmed via direct DB query: went from 6 Headlight Assembly Parts to 5, the source Part row is gone, the target now has both PartImages, and its returned grade/damage codes are the union of both (`faded_paint`/`surface_wear`/`scuff` + `paint_fade`/`wear`/`scratch`) exactly as Phase 1's aggregation logic promises.
- **Not verified in a real browser**: the sandboxed Chrome automation tool in this environment could not reach `localhost`/`127.0.0.1` (consistent `SecurityError`/error-page failures on every attempt, unrelated to the app itself -- confirmed the dev server was healthy via `curl` the whole time). UI correctness instead relies on the React Testing Library coverage above, which does render real DOM output and assert on real click/API-call wiring, plus the direct API-level live check.

**Environmental gotcha hit and fixed along the way, worth remembering:** killing a background test run via the task-stop mechanism does not reliably kill the underlying `node`/`jest`/`next` child process tree on this Windows machine (same class of issue as the orphaned-process incident documented in part 3 below) -- leftover BullMQ/ioredis connections from orphaned processes caused several minutes of confusing, non-reproducible `waitFor` timeouts across unrelated e2e files that looked like flaky infrastructure but were actually zombie workers racing for jobs with dead DB connections. Fixed by explicitly killing orphaned `node.exe` PIDs (`Get-CimInstance Win32_Process` + `Stop-Process`) and restarting the Redis container before trusting any e2e timing result again.

**Committed and pushed:** all of Phases 1-5 (this session's work plus the prior sessions' uncommitted Phase 1-4 work) landed in one commit, `f92cd2b` ("Ship Phases 3-5 of photo->part identity..."), pushed straight to `origin/main` (no PR -- user asked directly). 63 files, +5200/-317.

**Dev environment left running for the user, confirmed healthy:** `docker compose` (postgres + redis) was already up; started `npm run start:dev` (backend, port 3001) and `npm run dev:frontend` (Next.js/Turbopack, port 3000) in the background and confirmed both serve `200` via `curl` after startup, backend's Nest route table logged cleanly (including the new `/parts/:id/merge` route), frontend's Turbopack build reported `Ready`.

## Session 2026-08-22 (part 4): photo -> part identity problem -- validated research doc, shipped Phase 1 (grading aggregation). Read this first next time.

After part 3 shipped multi-part-per-photo detection, the user raised a real follow-on concern: a Part's AI grade only ever comes from **one** photo -- damage visible on a different angle of the same physical part is invisible. They asked for a plain-English summary of the problem + possible fixes (given, not repeated here -- see part 3's own summary reply in conversation), then went off and did their own research, coming back with an 8-step implementation doc (worker-tagged photo sections, per-section grouping, pgvector embedding-based matching, a Part<->Photo M:N schema change, multi-image grading, a manager merge tool) and explicitly asked for it to be **validated against the real code before implementing anything** -- "treat this document as a hypothesis to check against the repo, not as ground truth."

**Validated via EnterPlanMode + real investigation** (not just reading the doc), full findings + phased plan written to `zesty-zooming-lemon.md` (plan file, kept for continuity). Headline corrections found:
- **Only one capture path exists**, not two to compare -- the "Part-First" on-site taxonomy-picking flow the doc asked about was deleted entirely in an earlier session (2026-08-21 redesign). Every current photo-upload path is unstructured.
- **Reintroducing per-photo/section tagging would be un-doing a deliberate earlier simplification** -- this project already tried `exteriorPhoto:{angle}:{photoId}`-style tagging once and walked away from it for being too much yard-floor friction. Worth a deliberate go/no-go, not a default yes.
- **No new Part<->Photo many-to-many schema is needed** -- `PartImage.partId` already gives one Part many images (confirmed live: `assignPhotos()` already loops multiple photos onto one Part today for manual multi-select assign). The real gaps are pure logic: the auto-create loop always spawns a new Part per detection instead of ever attaching to an existing one, and grading has no concept of "the Part's grade" at all -- `ai_analyses` is uniquely keyed per `(part_image_id, model_version)`.
- **The "grading blind spot" had a near-zero-cost fix available immediately, decoupled from solving the harder duplicate/grouping problem**: `PartsService.list()`/`detail()` were picking only the *most recently graded* `AiAnalysis` row per Part and silently discarding every other image's already-computed grade/damage codes -- true today for any Part that already has multiple photos via manual assign, with zero new AI calls needed to fix.
- **The `embeddings` table + pgvector are real** (0.8.6 installed, correct schema/RLS) **but completely unused** -- 0 rows, 0 code calling it anywhere. Flagged a real risk before committing to it: this codebase's only AI integration today is Google's plain Generative Language REST API (simple API key); true multimodal/image embeddings are commonly a separate Vertex AI product with different auth/billing -- worth confirming before assuming it's a drop-in extra call.

**Recommended phasing** (full detail in the plan file): Phase 1 (fix the grading discard -- ships alone, no schema/AI-cost change) -> Phase 2 (lightweight batch section tag on capture) -> Phase 3 (rescope the debounced job per-section + add grouping to the prompt) -> Phase 4 (one Part per confirmed group, reusing existing `addImageInTransaction` -- no new code needed there) -> Phase 5 (manager "merge duplicate parts" tool, ship regardless of how good 2-4 get). Embedding/pgvector-based matching kept as a **deferred, not decided-against** backstop within Phase 3, gated on confirming the real API/cost surface first.

**User's call on scope (asked directly via AskUserQuestion): Phase 1 only, for now.** Phases 2-5 stay validated-but-deferred in the plan file until a separate go-ahead.

**Phase 1 -- shipped and verified this session:**
- `PartsService`: new `aggregatePartAnalysis()` helper (`backend/src/parts/parts.service.ts`) -- given all of a Part's `AiAnalysis` rows (ordered latest-first, matching every caller's existing query), picks the **worst-graded** row as the "controlling" analysis (same id/confidence/status the UI and `recordCorrection()` already act on -- never a synthesized fake row) but reports the **union** of every row's damage codes. Falls back to the latest row when none have a grade yet (all pending/failed) -- byte-identical to the old behavior in that case, so single-photo Parts (the overwhelming majority today) see zero behavior change. Wired into both `list()` (now groups `analyses` by `partId` before aggregating, was "keep first seen") and `detail()` (was `analyses[0] ?? null`).
- **Evidence:** new e2e test in `parts-listing.e2e-spec.ts` -- a Part with 2 photos (one A/clean, one C/damaged, damaged one saved *second* to prove it's picking worst-grade not latest-row) asserts the list AND detail endpoints both return grade C with the union of both photos' damage codes (`['rust','scratch']`), and that the returned analysis `id` is the real C-row's id (not synthesized) so correction-recording still has a valid target. 7/7 in `parts-listing.e2e-spec.ts` alone; full e2e suite 89/90 (the 1 failure is the same pre-existing documented `app.e2e-spec.ts` ioredis teardown flake, unrelated); backend unit 20/20; lint/typecheck clean (2 prettier auto-fixes applied along the way). Dev backend restarted and confirmed healthy.
- No frontend changes needed -- `PartListItem`/`PartDetail`'s TypeScript shape is unchanged, this is a pure backend read-path fix.
- **Checked against the real BMW vehicle: no visible effect there yet, and that's expected.** None of its 71 parts currently have more than one `PartImage` (confirmed via direct query) -- Phase 1 only fixes the *aggregation* once a Part has multiple photos; nothing today's auto-create loop does yet attaches more than one photo to a single Part (that's Phase 4, not built). This fix is live and correct for any Part a manager multi-select-assigns manually, and is the foundation Phase 4 will rely on later -- but don't expect the real Inventory view to visibly change until Phase 4 ships.

**Phase 2 -- shipped and verified this session too** (user said to continue past Phase 1 and to update this doc after each phase from here on):
- New nullable `section` column on `VehiclePhoto` (migration `1787490000000-AddVehiclePhotoSection`, new `vehicle_photo_section` Postgres enum: `front`/`rear`/`driver_side`/`passenger_side`/`interior`/`undercarriage`). Deliberately **not** reusing the existing `vehicle_image_angle` enum/`VehicleImage` entity found during investigation -- confirmed via grep that entity is completely dead code (only self-referenced + the entity registry, zero real usage anywhere) left over from the deleted Part-First 4-required-angle flow, and its value set (`front/rear/left/right`, all mandatory) doesn't fit this feature's "optional, driver/passenger-not-left/right, batch-level" design.
- Tag is **one per upload batch, never per photo, never required** -- deliberately the lightest version possible given finding #2 from Phase 1's validation (this project already tried per-photo angle tagging once and walked away from the friction). Threaded through the one shared `VehiclesService.savePhotos()` helper both `intake()` and `addPhotos()` already call, so both the initial-intake and add-more-photos backend paths support it identically.
- New `AddVehiclePhotosDto` (`POST /vehicles/:id/photos` had no body DTO at all before now); `VehicleIntakeDto` gained the same optional `section` field.
- Frontend: `my-vehicle-detail-page-client.tsx` (the "add more photos" mobile screen) gained a row of optional section chips shown once photos are staged, resets after a successful upload. `VehiclePhotoSummary`/`VehiclePhotoListItem` expose `section` end to end.
- **Deliberate scope trim, flagged not hidden:** did **not** wire a section picker into the *initial* intake capture screen (`vehicle-page-client.tsx`) -- that path stages photos into an offline IndexedDB draft and uploads later via `sync.ts`'s `buildIntakeFormData()`, a materially different (queued/offline) serialization path than the direct synchronous "add more photos" upload. The backend already accepts `section` on `POST /vehicles/intake` too (same DTO field, same `savePhotos()`), so wiring up that UI later needs no backend work -- just touching the offline draft type + sync serializer, deliberately not attempted in this pass to avoid touching the background-sync/IndexedDB layer without dedicated attention.
- **Evidence:** 2 new e2e tests in `vehicle-photos.e2e-spec.ts` (batch tag applied to every photo in that request and left null when omitted; an invalid section value 400s). Full e2e suite 91/92 (same pre-existing unrelated ioredis flake); unit 20/20; both workspaces lint/typecheck clean (2 more prettier auto-fixes along the way). New frontend tests in the mobile add-photos page spec (chip selection reaches `addVehiclePhotos()`, resets after upload) -- full frontend suite 182/182. Dev backend restarted, both servers confirmed healthy.

**Not done / staying deferred (Phases 3-5):**
- Per-section scoped analysis job + AI-driven grouping, embedding-based matching, and the manager merge tool are designed in the plan file but not started -- continuing to Phase 3 next in this same session per the user's instruction.
- The initial-intake capture screen's section picker (see trim above) -- can be picked up independently whenever, backend is already ready for it.
- Nothing from today (parts 1-4) is committed or pushed yet.

## Session 2026-08-22 (part 3): multi-part detection per photo -- the real fix for "I don't see all the other parts."

User checked Inventory after part 2's work and only saw 4 parts (Hood, Seat Front, Seat Rear 2nd Row, Fender) -- explicitly asked for "literally all of them," calling out headlights specifically. Root cause: `GeminiService.buildVehicleGradingPrompt()` told Gemini to return **exactly one suggested part per photo**. With 12 real photos, that's a hard ceiling of 12 possible parts, and a single wide-angle photo showing a headlight *and* bumper *and* fender together only ever got labeled as one of them -- headlights (already in the taxonomy, already flagged photo-identifiable) never had a chance to surface.

**Fix, planned via EnterPlanMode and approved before building** (plan file: `zesty-zooming-lemon.md`):
- `gemini.service.ts`'s prompt reworded to ask for **one entry per (photo, distinct visible part) pair** instead of one per photo -- a photo can now produce several `photo_suggestions` entries sharing the same `photo_index`. No Zod schema change needed at all (`GeminiPhotoSuggestionSchema` already permitted this shape).
- New `vehicle_photo_suggestions` table (migration `1787480000000-CreateVehiclePhotoSuggestions`) replaces the old single `suggested_taxonomy_id`/`suggested_confidence` columns on `VehiclePhoto` (dropped in the same migration) -- a photo can have many suggestions now, one row each, unique on `(vehicle_photo_id, taxonomy_id)`. That unique constraint doubles as the new auto-create idempotency gate (was an in-memory `photo.suggestedTaxonomyId === null` check, now a real per-pairing DB lookup in `AiAnalysisService.analyzeVehicle()`).
- `VehiclesService.listPhotos()`, the frontend `VehiclePhotoSummary` type, and `vehicle-detail-page-client.tsx`'s photo grid all updated from a single suggestion field to a `suggestions[]` list; the single-photo-select taxonomy prefill now picks the highest-confidence suggestion instead of the only one that used to exist.

**Live-verified against the real BMW vehicle** (not just e2e mocks): re-triggered analysis, and the single pass went from 4 parts to **71 parts across 25 distinct taxonomies** -- including Headlight Assembly (the specific thing the user asked for), Front/Rear Bumper Cover, Grille, Windshield, Tail Light, Quarter Panel, Front/Rear Door, Rocker Panel, Back Glass, Deck Lid Assembly, Wheel, Emblem, Front Door Mirror, plus interior parts (Steering Wheel, Dash Panel, Console Front/Rear, Instrument Cluster). Confirms the core fix works end-to-end.

**Known, explicitly-scoped-out tradeoff (told to the user in the plan, not a surprise):** no dedup *across different photos* of the same physical part -- e.g. "Headlight Assembly" was suggested from 5 different photos and became 5 separate Parts, same for Hood (5x) and Fender (6x). Real ambiguity (same headlight from two angles, or actually driver + passenger headlights?) isn't solvable from image content alone; a manager sorts it out in Review Queue like any other correction. **This is the most likely next thing the user will push back on** once they actually look at the 71-part list -- worth proactively raising next session if they haven't already, rather than waiting to be asked.

**Evidence:** backend e2e (`vehicle-analysis.e2e-spec.ts`, 3 tests rewritten/added: multiple parts from one photo, below-threshold-stays-hint, re-suggesting the same pairing refreshes confidence but never re-creates a Part even if the new confidence crosses threshold) -- 88/89 full e2e suite (the 1 failure is the same pre-existing documented `app.e2e-spec.ts` ioredis teardown flake, unrelated). Backend unit 20/20, frontend 181/181 (2 new tests in `vehicles/[id]/page.test.tsx` for the suggestions-array shape). Both workspaces lint/typecheck clean.

**Also this session: found and fixed the actual cause of a recurring "Jest worker encountered N child process exceptions" error the user kept hitting.** Two distinct causes, both real, both fixed:
1. 16 orphaned Node processes (5 dead `nest start --watch` trees, 2 dead Jest e2e runs) had piled up from earlier restarts this session that never actually terminated -- only their tracked wrapper task died, not the underlying Windows process tree. Killed by PID via `Get-CimInstance Win32_Process` inspection.
2. The real, durable fix: this machine has 16 logical CPUs, so Jest's default `maxWorkers` (`numCpus - 1` = 15) could spin up 15 parallel `ts-jest` compiler processes on a machine that's chronically low on free RAM (Chrome regularly using 3.5-4GB across 40+ processes, a WSL VM sitting idle at ~900MB). Added `"maxWorkers": 2` to `backend/package.json`'s Jest config -- applies to `npm test`, `test:watch`, and any editor Jest extension reading the same config.

**Not done / open follow-ups:**
- The duplicate-across-photos tradeoff above -- not a bug, but worth watching for user reaction.
- Nothing from today (parts 1, 2, or 3) is committed or pushed yet.
- `GEMINI_API_KEY`/live-grading status not re-checked this part of the session.
- Chrome tab cleanup (was ~3.8GB across 43 processes) still pending on the user's end -- flagged, not acted on.

## Session 2026-08-22 (part 2): auto-create parts from confident AI suggestions + Review Queue upgrades.

User feedback after seeing the per-part grades on the vehicle detail screen (built in part 1, below): approving/grading a part still required a manager to manually select photos and click "Assign" before AI ever ran on it, even when AI could already tell what the part was with high confidence. Ask: AI should identify and grade as many parts as it confidently can straight from the photos, land them in the Review Queue automatically, and a manager should be able to (a) see the source photo AI used, (b) re-grade a part on demand, and (c) manually add a part that has no photo at all (e.g. an alternator inside an unphotographed engine bay).

**1. Auto-create Parts from confident suggestions.** `AiAnalysisService.analyzeVehicle()` already wrote a `suggestedTaxonomyId`/`suggestedConfidence` hint onto each `VehiclePhoto` (part 1's work). Now, for every photo whose suggestion confidence is ≥ the tenant's `aiConfidenceThreshold` (same knob Review Queue's `needsReview()` already used, default 0.7), it automatically does what a manual "Assign" click used to require: creates a `Part` + turns the photo into a `PartImage` via `PartsService.addImageInTransaction()` (which already enqueues the AI grading job). Below-threshold suggestions are unchanged — still just a hint requiring a manual Assign.

**2. Broadened AI-identifiable parts to include interior, not just exterior.** `taxonomy.seed.ts`'s classification pattern (backing the `isExteriorVisual` column, name kept as-is to avoid migration churn even though the meaning is now "photo-identifiable") now also flags seats, dash panel, console, steering wheel, headliner, door trim panels, instrument cluster. Re-seeded: 236 taxonomy rows now flagged (up from 219 exterior-only) out of 706 total.

**3. Manual part add with no photo.** New `PartsService.createManual()`/`createManualInTransaction()` (status `NEEDS_MANUAL_GRADING`, no `PartImage`, no AI job — there's no photo to grade) and `POST /vehicles/:id/parts` (manager/owner). Review Queue got an "Add a part" panel: search/pick a vehicle, search/pick a taxonomy (reuses the vehicle detail page's existing taxonomy-search pattern), submit.

**4. Photo thumbnail + re-grade in Review Queue.** New `GET /parts/:id/images/:imageId/file` (mirrors the vehicle-photos file route) and `PartsService.getImageFile()`. New `PartsService.regrade()` + `POST /parts/:id/regrade` — force-regrades every `PartImage` on a part, bypassing the normal "already complete, skip" idempotency check (new `force` flag threaded through the job data and `analyzePartImage()`), upserting onto the same `(partImageId, modelVersion)` row rather than duplicating. Frontend: new `PartImageThumb` component (mirrors `VehiclePhotoThumb`'s authenticated-blob pattern), rendered per Review Queue card; a "Re-grade" button next to Approve (shown only when the part has photos).

**Real bug found + fixed, not a business-logic issue:** the first attempt at all of the above failed every new e2e test with `Nest can't resolve dependencies of the PartsService ... "BullQueue_default"`. Root cause was a genuine 3-way circular import at the ES-module level: `ai-analysis.service.ts` → `parts.service.ts` (for its new `PartsService` dependency) → `ai-analysis.processor.ts` (for the `AI_ANALYSIS_QUEUE` constant) → back to `ai-analysis.service.ts`. Node's CommonJS module cache returned a still-incomplete `exports` object partway around that cycle, so `AI_ANALYSIS_QUEUE` was `undefined` at the exact moment one of the decorators evaluated it — registering/injecting BullMQ's default unnamed queue instead of the real one. **Fixed** by extracting the constant/type into a new dependency-free `backend/src/ai/ai-queue.constants.ts` that both `parts.service.ts`/`parts.module.ts` and `ai-analysis.processor.ts` import from directly, breaking the cycle for good (not a `forwardRef()` band-aid).

**Evidence:** new `backend/test/parts-manual-and-regrade.e2e-spec.ts` (6 tests: manual add + no-AI-call, the file route's 200/404/401, force-regrade actually overwriting a prior `COMPLETE` analysis without duplicating the row) — 6/6 once the circular import was fixed. `vehicle-analysis.e2e-spec.ts` (extended with an auto-create-on-confident-suggestion assertion and a below-threshold-stays-manual test) + `parts.e2e-spec.ts` — 9/9. Full backend unit suite — 20/20. Full frontend suite — 180/180 (32 files), including 5 new Review Queue tests (thumbnail shown/hidden, re-grade busy state, re-grade button hidden for a photo-less part, full add-a-part flow). Both workspaces typecheck and lint clean.

**Not done:**
- **Live browser verification never happened this session** — the Claude-in-Chrome extension wasn't connected, so the one item in the plan's own "Verification" section that needed an actual browser (confirm a vehicle with confident suggestions auto-creates real Parts with a visible photo, no manual Assign click) is still outstanding. Both dev servers were confirmed live (`curl` health check) throughout.
- Nothing from today (either part 1 or part 2) is pushed, or even committed, yet — `git status` shows the full diff still sitting in the working tree.

### What's left for tomorrow
1. **Live-verify Review Queue in an actual browser** — reconnect Claude-in-Chrome (or do it manually): upload a real vehicle's photos, confirm confident suggestions (Fender/Hood/Quarter Panel/Rear Bumper Cover were seen at 0.75–0.92 confidence earlier this session, all above the 0.7 default) land as real Parts in the Review Queue with a visible thumbnail and no manual Assign click; click Re-grade on one and confirm it actually re-calls Gemini; add a manual part with no photo via the new panel.
2. **Commit and push.** Nothing from today is committed. Review the full diff (`git status`/`git diff`) before staging — it spans both part 1 (whole-vehicle grading) and part 2 (auto-create + Review Queue) of today's session.
3. **Car-Part.com integration** — see the dedicated plan below. Next concrete action is a phone call/email to Car-Part.com sales, not more building; nothing in this app-side codebase blocks starting that conversation today.
4. `GEMINI_API_KEY` status carried over from part 1 — confirm whether it's been added yet; if not, real grades (vs. the correctly-handled `failed` status) still won't appear.
5. `docs/BUILD_PLAN.md` still doesn't reflect the vehicle-photos/auto-create redesign (carried over from earlier sessions) — not urgent, but the gap has now grown by two more sessions' worth of shape changes.

## Car-Part.com Integration — Requirements Gathering Plan (2026-08-22)

**Why this exists:** while building out Review Queue, the user asked about pushing approved inventory straight to a real marketplace. Car-Part.com (the dominant recycled-auto-parts marketplace/DMS vendor) came up specifically. The user confirmed they don't currently have a Car-Part.com account of any kind (**"i odnt hvae carparts .com"**) and didn't answer a follow-up question about eBay developer credentials — so **both marketplace integrations are blocked on the user obtaining external access first**, not on anything left to build in this codebase. This section is the concrete checklist for what to go get, researched today against Car-Part.com's own public site (`products.car-part.com`) rather than guessed.

### What's confirmed (from Car-Part.com's own public pages today)
- Car-Part.com's own inventory/DMS product is called **Checkmate** — a full on-premises Windows server+workstation application (Windows 11/Server 2022+, 16–32GB RAM server, etc. — this is a locally-installed system, not a cloud API). Related first-party tools: **Partmate** (portable inventory-taking device app that syncs into Checkmate) and **Car-Part Exchange** (lets Checkmate yards broker parts from other recyclers/vendors already selling on Car-Part.com).
- Their core interchange/fitment data standard is the industry-standard **Hollander Interchange** data (confirmed independently — a competing third-party yard-management product, YardSmart, also licenses "Hollander Interchange data" for the same cross-reference purpose, which is the standard way U.S./Canadian salvage yards match which vehicles a given part fits).
- **No public API, data-feed spec, or "connect your own system" documentation exists anywhere on their public marketing site.** `products.car-part.com` (Checkmate, requirements, about) has zero mention of an API, FTP feed, or third-party interface program. The main `car-part.com` domain's rates/membership-terms pages (`rates.htm`, `recterms.htm`) blocked automated fetching (403) — a human visiting in a real browser, or calling, is the only way to see those.
- **Confirmed direct contact:** phone **(859) 344-1925**, sales inquiries **Sales@Car-Part.com**, general **info@Car-Part.com**.

### The honest implication
Car-Part.com's business is built around yards running *their* Checkmate software, which presumably handles marketplace listing internally. There is no evidence of a self-serve public API a third-party system like this one (JUNK app) could just call. That doesn't mean it's impossible — plenty of DMS vendors have an unadvertised "data exchange" program for exactly this case — but it means **the next step is a phone call, not more code.**

### Exact checklist — what to get from Car-Part.com before any integration code gets written
1. **Ask directly whether a non-Checkmate system can list inventory on Car-Part.com's marketplace at all**, and if so, what that path is called internally (this determines everything else — it may turn out the honest answer is "only via Checkmate," in which case the integration is really "buy/run Checkmate," not "write an API client").
2. **If a data-exchange path exists, get the exact feed format**: file-based (CSV/XML/fixed-width) on a schedule, or a real-time API/webhook? What authentication (API key, SFTP credentials, IP allowlist)?
3. **Exact field list and formatting rules** for a listing: required vs. optional fields (year/make/model/trim, part name — and specifically whether they require **Hollander interchange numbers** rather than free-text part names, which would mean this app needs a Hollander number mapped onto its own taxonomy), price, condition/grade vocabulary (does their grading scale match this app's A/B/C, or need translation?), quantity, location/yard ID, photo URLs or upload mechanism, and any required unique/SKU identifier for update-vs-create semantics.
4. **Image requirements**: hosted URL vs. direct upload, count limits, resolution/format requirements, and whether photos are mandatory per listing.
5. **Update cadence and semantics**: how a price/quantity change or a sold part gets reflected (delete, quantity-to-zero, explicit "sold" status?), and how often the feed is expected/allowed to run.
6. **Commercial terms**: signup cost, any per-listing or revenue-share fee, contract length, and whether a **Hollander interchange data license is a separate paid product** from marketplace listing access (these are commonly sold separately in this industry).
7. **Sandbox/test environment**: whether they offer a non-production endpoint or test account to validate a feed before it goes live against real buyers.
8. **Yard/dealer account prerequisites**: business license, physical yard verification, or other recycler-specific vetting that might gate signup before any technical work is possible.

### What this app already has ready, once the above is known
The data Car-Part.com would need already exists in this schema and mostly just needs an exporter once the target format is known: `Part` (taxonomy, status, grade via `AiAnalysis`), `Vehicle` (VIN, year/make/model/trim), `PartImage` (real photo bytes via `LocalFileStorage`), and tenant/yard identity via `Tenant`. The realistic first build, once Car-Part.com's actual answer to checklist item 1 is known, is a scheduled export job (approved Parts → their format) plus a mapping table from this app's `PartTaxonomy` to whatever part-naming/Hollander-number scheme they require — not a rebuild of anything that exists today.

### Also still open: eBay
A clarifying question about eBay developer credentials was asked earlier this session and never answered. eBay does have a real, public, self-serve Developer Program (api.ebay.com, OAuth-based) — worth revisiting once Car-Part.com's answer is in, since eBay may turn out to be the faster path to *some* marketplace presence while Car-Part.com's actual integration path is still being confirmed by phone/email.

## Session 2026-08-22: automatic whole-vehicle AI grading + manager dashboard redesign.

User feedback after the first real end-to-end usage (2026-08-21 session): the manager dashboard was a plain text list with no visible photos, and nothing was ever graded until a manager manually assigned raw photos to a part-taxonomy category (the only thing that triggered AI grading before this session). Wanted: upload photos → see a grade automatically, even from just 1-2 photos, plus a clearer parts-per-vehicle count.

**New, parallel whole-vehicle grading pipeline** (the existing per-part pipeline is untouched, still the only path to real marketplace inventory):
- New `vehicle_analyses` table/entity (migration `1787456000000-CreateVehicleAnalyses`) — mirrors `AiAnalysis` but scoped to a vehicle, no uniqueness constraint (a new row is written every time the photo set changes; "latest by createdAt" is the current grade, same pattern `PartsService.list()` already used for per-part grades).
- `GeminiService.analyzeVehiclePhotos()` — same REST call shape as `analyzePartImage`, sends every currently-available `VehiclePhoto` as its own `inline_data` part in one request; new whole-vehicle grading prompt, explicitly told 1 photo is enough.
- New `VEHICLE_ANALYSIS_QUEUE`/`VehicleAnalysisProcessor`, mirroring `AiAnalysisProcessor` exactly. `VehiclesService.intake()`/`addPhotos()` enqueue a job **after** their transaction commits (not from inside `savePhotos()` itself, where it started).
- `VehiclesService.list()`/`.mine()`/`.detail()` now return `latestGrade`/`latestVehicleAnalysis` + `firstPhotoId` (for a list-card thumbnail).

**Real bug found + fixed via live testing (not caught by e2e mocks):** enqueuing the job from inside `savePhotos()`'s still-open Postgres transaction let the BullMQ worker start `analyzeVehicle()` — its own separate transaction — before the photo insert had actually committed, so it could see zero or stale photos. Caught by a real e2e test flaking on a 2nd-photo upload. Fixed by moving the enqueue to after `withTenantContext()` returns in both `intake()` and `addPhotos()`.

**Second real bug found via the full e2e suite, not just the new spec in isolation:** adding a second persistent BullMQ `Worker` (`VehicleAnalysisProcessor`) without also closing it in `test/close-test-app.ts` reintroduced the exact documented "Connection is closed" ioredis teardown race this project already fixed once for the first worker (see the Phase 4 "Test-infrastructure note" below) — and made it noticeably worse (previously ~1-in-8 runs, briefly ~4-in-16 with two unclosed workers). Fixed by adding the same graceful `worker.close()` call for the new processor. After the fix, full-suite flake returned to roughly the pre-existing documented rate (~3-in-16 across repeated runs) — **not fully eliminated**, consistent with this project's own prior conclusion that this specific Windows/BullMQ/rapid-sequential-Nest-boot teardown timing issue isn't worth chasing further (already `continue-on-error` in CI). Every affected spec passes 100% in isolation.

**Live-verified against the real running dev servers** (not just mocked e2e): uploaded a real photo via `POST /vehicles/:id/photos` to the user's actual demo vehicle (VIN `KMHGN4JE1FU096946`), watched the job run, and got back a real `status: "failed"` `VehicleAnalysis` row with `photoCount: 1` — correct behavior, since `GEMINI_API_KEY` is still empty in `backend/.env`. **The grading pipeline is fully wired and will produce real grades the moment a real key is added** — nothing else needs to change. Cleaned up the synthetic test photo/row afterward.

**Frontend**: `(desktop)/vehicles/page.tsx` redesigned as photo-first cards (thumbnail, grade badge, parts-count badge, crush-status badge) instead of plain text rows. `vehicle-detail-page-client.tsx` gained a grade card (grade/confidence/damage codes, or "not graded yet"/"grading in progress"/"grading failed" states) above the photo grid; the existing photo-select-to-assign-taxonomy flow is unchanged underneath, now explicitly labeled as optional (only needed for marketplace part cataloging — grading no longer depends on it). New shared `components/desktop/vehicle-photo-thumb.tsx` and `grade-badge.tsx`.

**Not done / open follow-ups:**
- `GEMINI_API_KEY` is still empty — real grades won't appear until the user adds one themselves.
- No live-refresh — the detail screen needs its manual "Refresh" button clicked to see a grade land after upload (same pattern as every other screen's retry button).
- Nothing is pushed yet — check `git status`/`git log` first thing next session.

## Session 2026-08-21: first real live usage — found and fixed the pipeline's actual break, plus a worker-facing feature.

First time anyone (the user, on their own phone + this machine's browser) actually *used* the app live end-to-end since the vehicle-photos redesign (see the 2026-08-19/08-21 section below). Found real bugs a live human walkthrough catches that no test suite does; fixed all of them; then built out a real feature gap the user hit immediately after.

### Environment set up this session (still running, not code changes)
- Docker (Postgres+Redis), backend (`:3001`), frontend (`:3000`) all confirmed live.
- **LAN access for a phone**: frontend's `NEXT_PUBLIC_API_BASE_URL` set to this machine's LAN IP (`192.168.1.181`) via `frontend/.env.local` (gitignored, machine-specific — **not committed, must be recreated on any other machine**: `NEXT_PUBLIC_API_BASE_URL=http://<this-machine's-LAN-IP>:3001`). Two Windows Firewall inbound rules added (`JunkApp Dev Frontend/Backend (192.168.1.49)`, TCP 3000/3001, Private profile, scoped to the phone's IP specifically — **not** committed anywhere since it's OS state, will need re-adding on a fresh machine or if the phone's IP changes via DHCP).
- Demo login for reference: tenant `992b3474-3f53-4dd7-8af4-217cb673c99e` (**not** the older `eab0ca24-...` tenant id mentioned in the 08-02 session notes further down — that was a since-reseeded DB), worker "Demo Worker" PIN `1234`, manager `manager@demo-yard.local` / `manager-dev-password` (from `backend/src/database/seeds/dev.seed.ts`).

### Real bugs found + fixed via live usage (none of these had test coverage before today)
1. **`allowedDevOrigins` missing.** Next.js dev mode blocks cross-origin requests to its own JS bundles/HMR socket by default — the phone loaded the initial HTML fine but every JS chunk request was silently blocked, so nothing ever hydrated (looked like "only one static page, nothing works"). Fixed: `frontend/next.config.ts` now sets `allowedDevOrigins: ["192.168.1.181"]`.
2. **`crypto.randomUUID()` doesn't exist over plain HTTP on a non-localhost origin** (it's restricted to secure contexts) — crashed both live-camera capture and, initially, the file-upload path too (missed on the first pass, found again when the user re-hit it after uploading). Fixed with a real fallback, not a suppression: new `frontend/src/lib/uuid.ts` uses `crypto.getRandomValues()` (no secure-context restriction) to build an equivalent v4 UUID whenever `crypto.randomUUID` is absent. Same restriction also silently disables the camera (`useCamera` degrades to the existing file-picker fallback, already handled) and service-worker registration (`pwa.ts`, already a safe no-op) over plain HTTP — noted for the user, not something code can work around, since those APIs have no non-secure-context equivalent. Real HTTPS (e.g. a local `mkcert` cert) is the actual fix if camera/offline-install matters on a phone; not done this session.
3. **`PhotoPicker`'s file input couldn't multi-select** — no `multiple` attribute, and its change handler only ever read `files[0]`. Fixed: added `multiple`, loop over every selected file (one `onFileSelected` call each, so every existing caller's single-photo-at-a-time logic just works unchanged). New test proves 2 files → 2 calls.
4. **PWA reload loop reappeared** (a previously-documented recurring dev-mode issue, see the "Recurring dev-mode nuisance" note far below) — a stale service worker from earlier testing raced the page load into an infinite reload on both the phone and this machine's browser. Not a code bug to fix (the registration-timing fix from the earlier session is still correct); walked the user through clearing site data (`chrome://settings/content/all`, or Application → Clear site data in DevTools) to purge the stale SW/cache, which resolved it.
5. **Hydration mismatch on `<body>`** — a false alarm: the "Monica AI" browser extension injects `monica-id`/`monica-version` attributes onto `<body>` before React hydrates. Fixed the documented, correct way for exactly this class of issue: `suppressHydrationWarning` on `<body>` in `frontend/src/app/layout.tsx` (scoped to that one element's own attributes only, not its children — doesn't hide real mismatches elsewhere).
6. **The actual product bug: "Finish & queue for sync" never sent anything.** `queueForSync()` only ever set local IndexedDB status to `"queued"` — the real network POST only fired via the `online` event listener or a manual visit to `/sync` → "Sync now". Since Background Sync API silently never registers over plain HTTP (see #2), and nobody had reason to discover the separate `/sync` screen, a worker tapping Finish got sent home with their vehicle going nowhere, no error, no indication anything was wrong. This is what "trying to send a vehicle right now, and it isn't working" actually was. **Fixed**: `handleFinish()` (`vehicle-page-client.tsx`) now attempts a real send immediately after queuing, and shows an inline error (draft stays queued for automatic/manual retry, nothing is lost) if it fails instead of silently navigating away. New tests mock at the `createFetchSyncClient` boundary rather than raw `fetch`/`FormData` — building a real multipart body against jsdom's `FormData.append` hit a jsdom-vs-Node dual-`Blob`-class incompatibility that's a test-environment-only artifact (confirmed via the actual jsdom stack trace), never a real issue in an actual browser (single `Blob` implementation) — worth knowing about if a future test needs to exercise `sync.ts`'s real wire format.

### New feature this session: worker-facing vehicle history + incremental photos
The user's real workflow needs more than a one-shot intake wizard: see vehicles they've already sent, and add more photos to one later ("send them out with whatever pictures I have"). Scoped with the user first (asked, not assumed): **own vehicles only** (not the whole tenant's), and **AI grading stays manager-triggered** via the existing photo-assignment step on desktop (`/vehicles/[id]` → assign photos to a taxonomy) — sending more photos does not bypass that.

- **Backend**: new `created_by_user_id` column on `vehicles` (migration `1787370332318-AddVehicleCreatedByUser`, nullable + `ON DELETE SET NULL` so removing a worker account later never cascade-deletes their intake history), set on every `/vehicles/intake` call. New `GET /vehicles/mine` (worker/manager/owner) — the caller's own vehicles, most recent first, with `partsCount` + `unassignedPhotosCount` per vehicle. New `POST /vehicles/:id/photos` (worker/manager/owner) — append more raw `VehiclePhoto` rows to an *existing* vehicle any time after intake, reusing `intake()`'s own storage+insert logic via an extracted `savePhotos()` helper. `GET /vehicles/:id`, `GET /vehicles/:id/photos`, and `GET /vehicles/:id/photos/:photoId/file` are now worker-accessible too (previously manager/owner-only) so a worker can actually view what they sent; `POST /vehicles/:id/photos/assign` (the AI-triggering step) deliberately stays manager/owner-only, unchanged.
- **Frontend**: mobile home screen (`(mobile)/page.tsx`) now has a "Your sent vehicles" section below in-progress drafts, fetched from `/vehicles/mine`, real loading/error/empty states. New `(mobile)/my-vehicles/[id]` screen — view a sent vehicle's info + existing photos (thumbnails via the same authenticated-blob pattern as the desktop assignment screen), camera or file-picker to add more, each upload sent immediately via the new endpoint (not queued through the offline-draft system, since the vehicle already exists server-side). Also relaxed the intake flow's "at least 1 photo" requirement on Finish — the backend already accepted 0-photo drafts, only the frontend button gate was blocking a true "send whatever I have, even nothing yet" case.
- **Evidence**: new backend e2e coverage (`vehicles-intake.e2e-spec.ts`'s new `GET /vehicles/mine` describe block: cross-worker isolation, counts correctness; `vehicle-photos.e2e-spec.ts`'s new `POST /vehicles/:id/photos` describe block: 401, 404 on an unknown vehicle, appends without touching existing photos) plus updated RBAC coverage on the now-worker-accessible photo routes. New frontend tests for the sent-vehicles list, the detail/add-photos screen, and the immediate-send behavior (success + failure paths). Full backend e2e suite run 4x this session for confidence: 76/76, 76/76, 68/76 (a burst of the pre-existing documented BullMQ/Redis teardown flake, see the "Test-infrastructure note" further below — landed on random suites, never once on the new code), 75/76 (single instance of the same flake) — consistent with that flake's already-documented profile, not a regression. Frontend: 163/163. Both workspaces lint clean (0 errors). Live curl smoke test against the real running backend + Postgres: worker synced a 0-photo vehicle, added a photo afterward via the new endpoint, confirmed it shows up correctly in `/vehicles/mine` with the right counts.
- **`backend/uploads/`** (real uploaded-photo bytes from this session's manual/smoke testing) was untracked and not gitignored — added to `.gitignore`; it's runtime storage, not source.

### What's left for tomorrow
1. **Nothing is pushed yet** — everything above is local commits only (or, if this session ends before a commit, still working-tree changes — check `git status`/`git log` first thing).
2. **HTTPS for real phone testing.** Camera capture and PWA/offline-install still don't work on the phone (secure-context restriction, see bug #2 above) — worth setting up a local cert (`mkcert` + `next dev --experimental-https`, or a reverse proxy) if testing the *actual* camera/offline flow on a real device matters, versus the file-picker fallback being good enough for now.
3. **Firewall rules are pinned to one IP.** If the phone's `192.168.1.49` address changes (DHCP), the two `JunkApp Dev *` rules need updating or widening to the `192.168.1.0/24` subnet.
4. **The real intake→AI pipeline still hasn't been walked end-to-end by an actual human on an actual phone** — today's session fixed what was blocking that (the send bug), but nobody has yet: sent a real photo from a real phone → had a manager assign it to a taxonomy on the desktop screen → watched it come back graded in the Review Queue. Worth doing first next session now that the blocker is gone.
5. **`docs/BUILD_PLAN.md`'s phase docs below don't reflect the vehicle-photos redesign or this session's worker-vehicle-history addition** — they still describe the original Phase 3/5 shapes (part-picking on-site, single desktop-only vehicle list). Not urgent, but worth a pass if the plan doc needs to stay authoritative.

## ✅ Closed 2026-08-19: `POST /vehicles/intake` implemented — the sync gap below is fixed

Found 2026-08-02, late session, while helping the user debug their own stuck real-world draft (VIN `KMHGN4JE1FU096946`, tenant `eab0ca24-451f-48e7-a4ae-8de8a680a115`): **the mobile app's sync call has no matching backend route.**

`frontend/src/lib/offline/sync.ts`'s `createFetchSyncClient()` POSTs a multipart form (`draftId`, `vin`, `vinEntryMethod`, `decoded`, `parts` JSON, per-photo files named `exteriorPhoto:{angle}:{photoId}` / `partPhoto:{partId}:{photoId}`) to `${apiBaseUrl}/vehicles/intake`. **That route does not exist anywhere in the backend** — `VehiclesController` only has `GET /vehicles` and `GET /vehicles/:id` (both manager/owner-only). The only related endpoint, `POST /parts/:partId/images`, uploads a photo to a part that *already exists in the database* — nothing ever creates that vehicle or those parts. Live-reproduced: `Sync now` on `/sync` → `POST http://localhost:3001/vehicles/intake` → real `404`.

**Fixed 2026-08-19.** `VehiclesController` now has `POST /vehicles/intake` (open to worker/manager/owner — overrides the controller's class-level manager/owner-only `@Roles`, same "any authenticated role" shape as `PartsController.uploadImage`). `VehiclesService.intake()` parses the multipart draft exactly as `buildIntakeFormData()` sends it (`decoded`/`parts` are JSON strings, validated with new Zod schemas in `vehicles/dto/vehicle-intake.schema.ts`; a malformed or missing 17-char `vin` is rejected with 400, matching the real mobile UI's own VIN validation) and, in one `withTenantContext()` transaction: creates the `Vehicle` row, one `VehicleImage` per exterior photo, one `Part` per `parts[]` entry, and one `PartImage` per part photo — the last of these via a new `PartsService.addImageInTransaction()` (an extraction of `addImage()`'s existing storage-write + DB-insert + AI-queue-enqueue logic onto a caller-supplied `EntityManager`, so the whole intake stays one atomic transaction instead of N separate ones). Idempotency: a new `vehicles.intake_draft_id` column (migration `1787180822883-AddVehicleIntakeDraftId`, partial unique index on `(tenant_id, intake_draft_id) WHERE intake_draft_id IS NOT NULL`) means re-POSTing the same `draftId` — exactly what `syncPendingDrafts()` does on a `sync_failed` retry — returns the already-created vehicle instead of duplicating it.

Real evidence, not just "tests pass": new `backend/test/vehicles-intake.e2e-spec.ts` (7 tests) builds the actual multipart request shape byte-for-byte the same way `buildIntakeFormData()` does, against a real Nest app + Postgres + Redis + BullMQ worker (Gemini mocked, same pattern as `parts.e2e-spec.ts`) — asserts the Vehicle/VehicleImage/Part/PartImage rows land correctly, files hit disk, the AI-analysis job the same `addImageInTransaction()` enqueues actually reaches `COMPLETE`, a same-`draftId` retry doesn't duplicate anything, and malformed input 400s. Two clean full-suite runs (`npm run test:e2e --workspace=backend`, all 14 suites / 65 tests) confirmed green; a couple of runs in between hit the *pre-existing* documented BullMQ/Redis teardown race (see "Test-infrastructure note" further down) landing on an unrelated suite each time (`app.e2e-spec.ts` once, this new suite once) — consistent with that flake's existing profile, not a regression from this change. Backend unit suite (15/15), lint (0 errors), and `nest build` all stayed clean through the `PartsService.addImage`/`addImageInTransaction` split. Frontend needed zero changes — `sync.ts` already sent the right shape; frontend suite re-verified anyway (32 files / 160 tests, unchanged).

**Not yet done:** nobody has re-run the user's actual real-world stuck draft (VIN `KMHGN4JE1FU096946` mentioned above) through the now-working endpoint — worth doing next session for the first genuine live confirmation of a real photo becoming a graded, reviewable Part.

## Redesign 2026-08-21: intake no longer asks a worker to pick/photograph individual parts

Picked up this session's leftover uncommitted work (started but not written up or committed in an earlier session) and finished verifying it. The mobile intake flow changed shape from what Phase 3 originally built: a worker used to pick a part taxonomy on-site and photograph each part individually (`(mobile)/intake/[draftId]/parts` + its per-part `camera` screen — both **deleted** this session), then `/vehicles/intake` created the Vehicle *and* its Parts in one shot. Now a worker just photographs the vehicle itself (open-ended count of photos, no taxonomy picking on a phone in a noisy yard) and a manager assigns raw photos to a part/taxonomy afterward from the new desktop `/vehicles/[id]` screen.

- **Backend:** `/vehicles/intake` (`VehiclesController`/`VehiclesService.intake()`) now only creates the `Vehicle` + one `VehiclePhoto` per uploaded `photo:{id}` field (new entity/table, migration `1787193466132-CreateVehiclePhotos`, same RLS shape as every other tenant-scoped table) — no `Part`/`PartImage` is created here anymore, and the DTO/schema no longer accepts a `parts` field. New `GET /vehicles/:id/photos` (list), `GET /vehicles/:id/photos/:photoId/file` (JWT-guarded raw bytes), and `POST /vehicles/:id/photos/assign` (manager/owner only: takes `photoIds[]` + a `taxonomyId`, creates the `Part`, turns each photo into a real `PartImage` via a new `PartsService.addImageInTransaction()` — an extraction of `addImage()`'s storage+insert+AI-enqueue logic onto a caller-supplied `EntityManager` so the whole assignment stays one transaction — then deletes the consumed `VehiclePhoto` rows and their files). `intake_draft_id` idempotency (migration `1787180822883`) is unchanged.
- **Frontend:** mobile `(mobile)/intake/[draftId]/vehicle` is now the last intake step — captures vehicle photos (camera or `PhotoPicker` file/drag-drop) and "Finish & queue for sync" directly, no more parts step in between. `sync.ts`'s `buildIntakeFormData()` and `offline/types.ts`'s `VehicleDraft` dropped `exteriorPhotos`/`parts`/`PartDraft` in favor of a single flat `photos` array. New desktop `(desktop)/vehicles/[id]` screen: shows the vehicle's existing Parts, a grid of unassigned photo thumbnails (fetched as authenticated blobs, since the file route is JWT-guarded), a taxonomy quick-pick/search, and an "Assign N photo(s)" action.
- **Bug found + fixed this session:** `(mobile)/page.tsx`'s `nextStepFor()` (used both when tapping a resumable in-progress draft and, previously, right after VIN entry) still pointed at the now-deleted `/intake/{id}/parts` route — resuming any draft that already had a VIN would 404. Fixed to `/intake/{id}/vehicle`. Grepped both workspaces for any other reference to the deleted `parts`/`camera` mobile routes or the removed `PartDraft`/`exteriorPhotos` types/fields — none found.
- **Evidence:** new `backend/test/vehicle-photos.e2e-spec.ts` (7 tests: auth/RBAC on all three routes, tenant isolation on the list, raw file serving + 404 for a foreign photo id, empty-`photoIds` 400, assigning a photo id not on the vehicle 404s, and the real path — assigning 2 photos creates 1 Part + 2 PartImages, consumes/deletes the 2 VehiclePhotos, and both images reach `AiAnalysisStatus.COMPLETE` via a real BullMQ+mocked-Gemini round trip) — passes 13/13 together with `vehicles-intake.e2e-spec.ts` in isolation, every run. Full backend e2e suite (15 suites / 71 tests) passes except the pre-existing documented BullMQ/Redis teardown race (see "Test-infrastructure note" below) — reproduced 3 times this session, landing on a different suite each time (`app.e2e-spec.ts` twice, `vehicle-photos.e2e-spec.ts` once, both a "cross-file bleed" pattern already documented, not a regression from this change), consistent with its existing profile and already `continue-on-error` in CI. Backend unit (15/15), frontend unit (153/31 files — down from 160/32 after deleting the two parts/camera test files, otherwise unchanged), backend lint (0 errors), frontend lint (0 errors), `nest build`, and `next build` (Turbopack, confirms `/vehicles/[id]` is now a real route and `/intake/[draftId]/parts` no longer is) all clean.

This means **the "photos to inventory" pipeline — the entire product thesis — has never worked end-to-end, in any session, including the original build.** Every AI-grading/Review-Queue/Inventory acceptance check in Phases 4–6 below was verified via e2e tests that create `Part`/`Vehicle` rows directly through TypeORM repositories, or via the standalone `POST /parts/:partId/images` endpoint against a manually-seeded part — never through a worker's actual mobile sync. That gap was real but invisible until an actual human walked the actual golden path today.

**What's needed (not started — deliberately deferred to next session, see below):**
1. A new `POST /vehicles/intake` endpoint (`VehiclesController` + `VehiclesService`) that:
   - Parses the multipart draft (likely `AnyFilesInterceptor` — field names are dynamic: `exteriorPhoto:{angle}:{id}`, `partPhoto:{partId}:{id}`)
   - Creates one `Vehicle` row (`tenantId`, `vin`, `make`/`model`/`year`/`trim` from `decoded`, `decodedRaw` = `decoded.raw`)
   - Creates one `VehicleImage` row per exterior photo (stored via the existing `LocalFileStorage`)
   - Creates one `Part` row per entry in the `parts` JSON array (`taxonomyId`, default `PENDING_AI` status)
   - Creates `PartImage` rows for each part's photos and enqueues an AI-analysis job per image — this part can directly reuse `PartsService.addImage()`'s existing storage+enqueue logic, just called in a loop instead of once
   - **Needs an idempotency guard** (e.g. an `intake_draft_id` column on `Vehicle`, unique per tenant, new migration) — `syncPendingDrafts()` retries a `sync_failed` draft, so a request that succeeded server-side but whose response was lost must not create a duplicate vehicle+parts on retry
2. An e2e test (`vehicles-intake.e2e-spec.ts` or similar) that posts a real multipart request matching `buildIntakeFormData()`'s exact shape and asserts the Vehicle/Parts/Images/AI-job all land correctly
3. Re-sync the user's real stuck draft (already unblocked to "1 photo, ready to sync" this session — see below) once the endpoint exists, to get the first real live confirmation of a photo becoming a graded, reviewable Part

## Where things stand (end of session, 2026-08-02) — read this first next time

**All 7 BUILD_PLAN phases are complete**, plus a post-Phase-7 hardening pass. Every gap flagged along the way has since been closed with real evidence — there is no known open gap in the plan itself. Full detail for each phase is below; this section is just the fast-resume summary. **Except see the 🔴 section above, found after this summary was originally written — the sync endpoint gap is the real next priority, above everything below.**

**To resume local dev:**
```
npm run db:up                 # Postgres + Redis (Docker)
npm run dev:backend           # -> http://localhost:3001
npm run dev:frontend          # -> http://localhost:3000
```
Both now boot cleanly together with zero manual port juggling (see "Post-Phase-7 — Environment hardening" below for why that wasn't always true).

**What's actually left is not more building — it's using the app for real:**
1. ~~Nobody has clicked through the app in a live browser this whole build.~~ **Done (2026-08-02), see "Live browser walkthrough" below — and it wasn't clean: found and fixed two real bugs no test suite had caught.**
2. ~~Gemini AI calls have only been verified once, in Phase 4, when the API key was first enabled.~~ **Done (2026-08-02):** user attached billing to the GCP project, quota went from 0 to working, and a real `analyzePartImage()`-shaped call against `gemini-flash-latest` succeeded end-to-end (see Phase 4 below). Along the way, found the *pinned* default model (`gemini-2.0-flash`) had also since been retired by Google — switched the default to the `gemini-flash-latest` alias to stop this recurring. Still not exercised through the actual app (worker flow can't reach a real photo upload without a camera, see item 6), but the Gemini integration itself is now confirmed live end-to-end outside the app.
3. **Two design decisions were made unilaterally this session and never explicitly confirmed with the user** — low-risk, but worth knowing about:
   - Backend's default port changed from 3000 → 3001 (`backend/src/main.ts`) to stop colliding with the frontend. If anything external (a script, a bookmark, a proxy config) assumed 3001 was the port *before* this session, double check — otherwise this is just now the honest, working default.
   - `@ducanh2912/next-pwa` was removed (it silently did nothing under this project's Turbopack build) and replaced with a small hand-written `frontend/public/sw.js`. If there's ever a reason to reintroduce a Workbox-based build step, it would need `next build --webpack` (a real behavior change, not just a config tweak) since Turbopack doesn't support webpack plugins at all.
4. ~~No CI pipeline exists yet.~~ **Done (2026-08-02): `.github/workflows/ci.yml`**, pushed and actually run on GitHub — two parallel jobs (`backend`: pgvector+redis service containers, the same `docker/postgres-init/*.sql` scripts docker-compose uses locally, migrate, lint, unit, e2e, build; `frontend`: lint, unit, build, Playwright e2e). `frontend` is green. Getting `backend` green took 5 real CI iterations and surfaced a genuine bug no local run had ever hit: Vitest was picking up the Playwright specs in `frontend/e2e/` (fixed, `frontend/vitest.config.mts` now excludes it) and, bigger, the pre-existing BullMQ/Redis teardown race documented in Phase 4 below was crashing the *entire* Jest process on GitHub's Linux runners (not just failing one test) — confirmed by reading `node_modules/bullmq/dist/cjs/classes/redis-connection.js` directly (a permission restriction that blocked this investigation in the original Phase 4 session no longer applies). `backend/test/setup-e2e.ts` now recognizes that exact race (matched via `util.inspect()` on the caught error, not `.stack`/`.message` directly — two earlier attempts at this matcher both looked right locally but still missed in CI) and lets the run finish normally instead of crashing outright. **What's left, honestly:** with the crash fixed, the underlying flake is a real test failure (an `ECONNRESET` on `auth.e2e-spec.ts`'s pooled-connection concurrent test, sometimes collateral damage in whichever suite is running when the race fires) — and on GitHub's shared/throttled runners it turned out to be *near-deterministic*, not the ~1-in-8 documented locally: 3/3 retry attempts failed identically in the run that settled this. Retrying further wasn't going to fix a resource-contention issue, so backend e2e is now `continue-on-error: true` in CI — it still runs and reports on every push, it just doesn't block the pipeline (frontend and every other backend step, lint/unit/build, still gate normally). This was a deliberate user decision (asked directly rather than assumed), not a silent weakening — the TODO comment in `ci.yml` names the real next lever if anyone picks this back up: Node HTTP keep-alive/timeout behavior (or `DB_POOL_MAX=2` contention) under CPU throttling. **CI is green as of run `30774617129`** (both `frontend` and `backend` jobs passing) — 7 pushed iterations start to finish, real bugs found and fixed along the way (Vitest picking up Playwright specs; the BullMQ crash-cascade), one pre-existing flake correctly identified as resource-contention and knowingly downgraded to informational rather than blocking forever or being silently patched away.
5. **Deployment has never been discussed or attempted.** Everything so far is local-only (Docker Compose Postgres/Redis, `npm run dev:*`). Hosting, environment secrets in production, and a real Postgres instance are all still open questions whenever that becomes relevant.
6. ~~The worker intake flow can't be walked past the vehicle-photo step in any headless/automated browser.~~ **Fixed (2026-08-02): `PhotoPicker` component** (`frontend/src/components/mobile/photo-picker.tsx`) — select a file or drag-and-drop, as an alternative alongside (not replacing) live `getUserMedia` camera capture. Wired into both the Vehicle exterior-photos screen and the per-part camera screen; reuses the exact same blur/lighting quality-analysis pipeline (`captureFromFile()` in `capture.ts`, sharing a `drawToCanvas()` helper with `captureFrame()`). Live-verified end-to-end via real file uploads. Found and fixed along the way: Tailwind's `sr-only` utility silently doesn't resolve in this project's v4 setup (first-ever use anywhere in the codebase) — replaced with the same visually-hidden pattern spelled out as explicit utility classes.
7. **Found and fixed (2026-08-02): a part stuck at 0 photos had no way back in.** A part is created the instant it's picked (before any photo exists) and immediately routes to its camera step; if that failed (camera unavailable) and the worker navigated away, the "Added" parts list rendered it as a plain non-interactive `<li>` — permanently stuck, un-revisitable, un-removable, silently blocking "Finish & queue for sync" forever (needs ≥1 photographed part). Fixed: each Added row is now a button that navigates back into that part's camera step, same as picking a new part does. Found and fixed while unblocking the user's own real stuck draft.
8. ~~See the 🔴 section at the very top of this file — the sync endpoint that ties all of the above together doesn't exist yet.~~ **Fixed (2026-08-19), see the ✅ section at the very top of this file.**

None of items 1–8 are blocking or broken on their own anymore — they're just the honest boundary of what "finished" means after a build-only session with no live browser and no production environment.

## Live browser walkthrough (2026-08-02)

First real click-through of the app in a live Chrome tab (`npm run dev:backend` + `npm run dev:frontend`, worker PIN login, manager email/password login, both against the seeded demo tenant `eab0ca24-451f-48e7-a4ae-8de8a680a115`). Found and fixed two real bugs; one environment limitation reconfirmed (item 6 above).

- **Bug found + fixed: PWA reload loop could prevent the app from ever rendering.** On a cold load (fresh browser profile / cleared SW+cache, i.e. exactly a real first-time visitor or first-time PWA install), the mobile app entered a rapid reload loop — the Next.js dev server log showed 20+ back-to-back `GET /` requests in a few seconds, `document.readyState` never reached `complete`, and the page never painted any content. Root-caused to `frontend/src/app/(mobile)/layout.tsx` calling `registerServiceWorker()` eagerly inside a mount-time `useEffect`, racing `sw.js`'s `skipWaiting()` + `clients.claim()` (Phase 7) against the same page's own in-flight JS chunk requests — confirmed by reproducing it twice and both times immediately fixing it by unregistering the service worker and clearing caches mid-loop. **Fixed** in `frontend/src/lib/pwa.ts`: registration is now deferred until the window `load` event (the standard documented pattern for this exact reason, per web.dev/Workbox guidance) instead of firing on mount. Re-verified live: a full cold reset (unregister SW, clear all caches, clear localStorage) followed by a fresh navigation now reaches `readyState: complete` and correctly lands on `/login`, with no loop. `frontend/src/lib/pwa.test.ts` (2/2) still passes unchanged.
- **Bug found + fixed: the desktop Vehicles screen was unconditionally broken since Phase 5.** Clicking "Vehicles" in the manager nav always rendered "Couldn't load vehicles." — never worked, with data or without. Root cause: `frontend/src/app/(desktop)/vehicles/page.tsx` hardcoded `listVehicles(token, { ..., pageSize: 200 })`, but the backend's `ListVehiclesDto` (`backend/src/vehicles/dto/list-vehicles.dto.ts`) caps `pageSize` at `@Max(100)`, so every request 400'd before ever touching the database. The screen's own unit test (`page.test.tsx`) mocks `listVehicles` directly, so it never exercised the real DTO validation and never caught this — exactly the class of bug a live walkthrough exists to catch. **Fixed**: lowered the hardcoded value to `pageSize: 100`. Re-verified live: the screen now correctly shows the "No vehicles in the yard" empty state instead of an error. `page.test.tsx` (4/4) still passes unchanged.
- **Verified working end-to-end, live, for the first time this build:**
  - Device→tenant binding screen, worker picker, PIN login (`1234` for Demo Worker) → lands on the Vehicles home screen with a real "Online" / "All synced" status bar.
  - "New Vehicle" → VIN entry screen → **live NHTSA decode** (not mocked): VIN `1HGCM82633A004352` correctly round-tripped as HONDA / Accord / 2003 / EX-V6 and prefilled the Vehicle details form.
  - Manager email/password login (`manager@demo-yard.local`) → all 8 desktop screens (Dashboard, Review Queue, Inventory, Vehicles, Marketplace, Analytics, Users, Settings) render against the real backend with correct empty states, no crashes. Users screen shows the real seeded Demo Manager/Demo Worker rows. Marketplace's "Export CSV" button fires a real `GET /parts/export.csv` (200).
- **Not reachable this session:** the worker flow's 4 exterior-photo capture step (see item 6 above — no camera in this environment), and therefore nothing downstream of it (AI grading, Review Queue with real data, approve→Inventory→CSV with a real part). The manager-side screens were exhaustively verified against their real empty-state paths instead.

## Phase 0 — Scaffold ✅ (2026-07-31)
- [x] npm workspaces for `/frontend` + `/backend`
- [x] TypeScript strict mode both workspaces
- [x] Tailwind + shadcn/ui in frontend
- [x] ESLint/Prettier shared config
- [x] Jest (backend) + Vitest/RTL (frontend), trivial failing→passing test round-trip verified (frontend)
- [x] Docker Compose: Postgres (pgvector image) + Redis
- [x] README with documented clone-to-running-app setup sequence
- [x] **Acceptance verified:** both dev servers boot (verified directly — `npm run dev` via cmd.exe wrapper loses buffered output on abrupt kill on Windows, direct `next`/`nest` invocation confirms clean startup), `npm test` passes both workspaces, `docker-compose up` brings up healthy Postgres+Redis, README sequence works up through Phase 0 (migrate/seed steps land in Phase 1)

## Phase 1 — Data Layer ✅ (2026-08-01)
- [x] TypeORM entities for all 11 tenant-scoped entities + Tenant + PartTaxonomy (`backend/src/database/entities/*.entity.ts`)
- [x] Migration written: `backend/migrations/1785559260000-InitialSchema.ts` — all tables, enums, indexes, FKs, RLS policies (`ENABLE` + `FORCE ROW LEVEL SECURITY` + `tenant_isolation` policy per tenant-scoped table)
- [x] `withTenantContext()` helper (`backend/src/database/tenant-context.ts`) — sets `SET LOCAL app.tenant_id` inside a transaction (UUID-validated, since SET doesn't accept bind params)
- [x] Taxonomy seed (`backend/src/database/seeds/taxonomy.seed.ts`) — 22 common salvage parts, 8 flagged quick-pick
- [x] Dev seed (`backend/src/database/seeds/dev.seed.ts`) — demo tenant + PIN worker + email/password manager
- [x] `DatabaseModule` wired into `AppModule`
- [x] RLS isolation e2e test written: `backend/test/rls-isolation.e2e-spec.ts` (SELECT/INSERT/UPDATE + no-context default-deny)
- [x] pgvector extension enabled
- [x] Migration applied against the corrected (non-superuser) `junkyard_app` role
- [x] Dev seed re-run against the corrected role
- [x] RLS isolation e2e test passing (4/4)
- [x] **Acceptance verified:** `npm run migrate`, `npm run seed:dev`, `npm run test:e2e` (rls-isolation, 4/4 passing) and `npm test` (unit) all green in `backend/`

### Resolved blockers (fixed 2026-08-01)
1. **RLS silently not enforced.** `docker-compose.yml`'s `POSTGRES_USER` (`junkyard`) is always created as a Postgres superuser by the official image's bootstrap, and Postgres never applies row-level security to superusers, even with `FORCE ROW LEVEL SECURITY`. Fixed by adding `docker/postgres-init/01-app-role.sql` (non-superuser role `junkyard_app`, granted DB + schema privileges) and pointing `DATABASE_URL` at it.
2. **`CREATE EXTENSION vector` needs superuser**, but the app now connects as non-superuser `junkyard_app`. Fixed by adding `docker/postgres-init/00-extensions.sql` (runs before `01-app-role.sql`, as the bootstrap superuser) and removing the `CREATE EXTENSION` call from the migration itself.
3. **`invalid input syntax for type uuid: ""` on the no-context RLS test.** Root cause: `app.tenant_id` is an undeclared custom GUC placeholder. Once `SET LOCAL app.tenant_id = '...'` runs at least once on a physical backend connection, Postgres resets it to `''` (not NULL) once the transaction ends — and with TypeORM's connection pool, a later query with no tenant context can land on that same reused connection, so `current_setting(...)::uuid` throws instead of returning NULL. Fixed by wrapping every RLS policy's `current_setting` call in `NULLIF(..., '')` before the `::uuid` cast, in both `USING` and `WITH CHECK` clauses.

Also fixed along the way:
- Test's cross-tenant VIN string was 19 chars against a `varchar(17)` column, masking the real RLS check with a length error — fixed to a 17-char value.
- `entities/index.ts` barrel re-exports enums alongside entity classes, which TypeORM's `entities:` array rejects (`Object.values(entities)` broke `data-source.ts`/`database.module.ts`) — fixed by adding `backend/src/database/entities.list.ts` with an explicit `ENTITIES` array of just the classes; both files now import from there instead.

## Phase 2 — Auth & Multi-tenancy ✅ (2026-08-01)
- [x] PIN login (Worker) via `POST /auth/login/pin` `{tenantId, userId, pin}` — `GET /auth/tenants/:tenantId/workers` lists worker names/ids for the PIN picker (never the PIN itself, see user.entity.ts)
- [x] Email+password login (Manager/Owner) via `POST /auth/login/manager` `{tenantId, email, password}`
- [x] Long-lived JWT (`JWT_EXPIRES_IN=30d`) — claims (`sub`, `tenantId`, `role`, `name`) are self-contained, `JwtStrategy.validate()` does no DB round-trip so an offline-launched PWA can still authenticate locally-cached requests
- [x] RBAC: `@Roles(...)` decorator + `RolesGuard`, e.g. `GET /auth/workers` (own-tenant worker list) is manager/owner-only
- [x] `JwtAuthGuard` registered globally (`APP_GUARD`) — every route requires a valid JWT unless annotated `@Public()`
- [x] Tenant-scoped queries go through the existing `withTenantContext()` helper (Phase 1) inside `AuthService`, never a bare repository call — `SET LOCAL app.tenant_id` is Postgres-guaranteed transaction-scoped, so there is no request-scoped "tenant context middleware" object that could itself leak; the guarantee lives at the DB layer instead
- [x] Pooled-connection RLS leak test: `backend/test/auth.e2e-spec.ts` forces `DB_POOL_MAX=2` and fires 30 concurrent alternating-tenant requests at `GET /auth/workers`, asserting every response only ever contains its own tenant's worker — passing
- [x] **Acceptance verified:** `backend/test/auth.e2e-spec.ts` (7/7) covers both login mechanisms (success + failure), `/auth/me` token validity + claims, RBAC denial (worker on manager-only route → 403), RLS scoping (manager only sees own-tenant workers), and the pooled-connection concurrent-tenant leak test. `npm test`, `npm run test:e2e` (12/12 across all 3 suites), `npm run build`, and `npm run lint` all clean.

### Design decisions made during this phase
- **Login requires a client-supplied `tenantId`**, for both PIN and password login. There's no cross-tenant email lookup (email isn't globally unique, and querying `users` without a tenant context would mean bypassing RLS for auth itself). This mirrors how the PIN flow already needed a known tenant to list workers from — in practice a yard's device/URL already knows which tenant it belongs to before anyone logs in. Tenant *identification* (slug, subdomain, QR code) is a Phase 3 frontend/UX concern, not solved here.
- **`DatabaseModule` is now `@Global()`** and exports `TypeOrmModule`, so `DataSource` is injectable anywhere (AuthModule today, every future resource module later) without re-importing it per feature module.
- **`DB_POOL_MAX` config** added to `DatabaseModule` (defaults to 10, same as node-postgres's own default) specifically so `auth.e2e-spec.ts` can force a tiny pool and reliably reproduce the pooled-connection leak scenario the Phase 2 planning-gate finding warned about.
- **Scaffolded root `GET /` → `GET /health`.** The placeholder Nest "Hello World" route had no reason to stay unauthenticated once `JwtAuthGuard` went global; replaced with a real `@Public()` health check instead of just exempting the placeholder.

## Phase 3 — Mobile Intake Flow 🚧 BUILT, acceptance not yet verified (2026-08-01)
- [x] Zustand store + IndexedDB + client-generated UUID dedup (`frontend/src/lib/offline/{db,store}.ts`)
- [x] NHTSA VIN decode client + Zod validation (`frontend/src/lib/nhtsa.ts`) — explicitly catches NHTSA's "200 OK, no make/model" undecodable-VIN shape, not just network failures
- [x] Blur/lighting quality check (`frontend/src/lib/offline/image-quality.ts`) — pure Laplacian-variance + mean-luminance function, fixture-tested, decoupled from canvas/video capture
- [x] Background Sync registration + `online`-event retry (`frontend/src/lib/offline/sync.ts`), wired up live in `(mobile)/layout.tsx` — full Workbox/service-worker build still deferred to Phase 7 by design
- [x] Auth screen (`(mobile)/login`) — device→tenant binding, Worker PIN / Manager email+password tabs
- [x] Home screen w/ sync status + empty state (0 pending) (`(mobile)/page.tsx`, `components/mobile/sync-status-bar.tsx`)
- [x] VIN Scanner + manual entry fallback (`(mobile)/intake/[draftId]/vin`) — manual entry is the only path when `BarcodeDetector` is unsupported (Safari/Firefox), not a secondary option nobody exercises
- [x] Vehicle Context screen + NHTSA decode + IndexedDB draft (`(mobile)/intake/[draftId]/vehicle`) — prefilled when decode succeeded, blank and fully editable (manual fallback) when it didn't; required 4-angle exterior photo capture with a per-angle blurry/dark warning
- [x] Part Selection w/ precached taxonomy (`(mobile)/intake/[draftId]/parts`, `GET /taxonomy`, `lib/offline/taxonomy-store.ts`) — quick-picks + search, "Finish & queue for sync" gated on at least one photographed part
- [x] Camera Capture + ghost overlay + blur/lighting validation (`(mobile)/intake/[draftId]/parts/[partId]/camera`) — ghost overlay is a generic centered framing guide, not a per-taxonomy reference silhouette (no such art exists yet; a real limitation, noted rather than silently skipped)
- [x] Sync Queue Manager screen + empty state (`(mobile)/sync`)
- [ ] **Acceptance NOT yet verified:** a live browser walkthrough of the full offline happy path (network disabled, VIN entry through sync) was not performed. All business logic — offline store, sync retry/pooled-leak-safety equivalents, blur/lighting detection, VIN decode/fallback, RBAC-gated taxonomy fetch — is covered by 98 frontend + 15 backend automated tests, and `next build`/`nest build` both succeed, but that stops short of the phase's literal acceptance criterion. Camera-dependent steps (4 exterior angles, per-part photos) can't be walked end-to-end without a real device/camera in this environment regardless — worth a manual pass on an actual phone before calling this phase done.

### Notes
- **Dynamic route pages are Server Component wrappers, not `use(params)` Client Components.** Every `[draftId]`/`[partId]` page (`vin`, `vehicle`, `parts`, `parts/[partId]/camera`) is a thin async Server Component that `await params` and passes plain string props to a `'use client'` child (`*-page-client.tsx`). Calling `use(params)` directly inside a client page suspended and never resolved under RTL in this workspace (npm workspace hoisting can produce duplicate `react` copies, a known cause of exactly this symptom) — the Server-wrapper split sidesteps it entirely and is also the more idiomatic Next 16 shape for a page that needs both a route param and client-side hooks. Use this pattern for any future dynamic route.
- **`react-hooks/set-state-in-effect`** (a stricter React 19 lint rule than most training data will know) fires on the classic "read browser-only state after mount, to dodge an SSR hydration mismatch" idiom — `useEffect(() => setState(readBrowserThing()))` — and also on "prefill local state once external data arrives" effects. Real fixes applied here, never suppressions:
  - Browser-only capability/storage reads: moved into the existing Zustand `hydrate()`/`hydrated` pattern (`useAuthSession.restored`, `useTenantStore`) or `useSyncExternalStore` (`navigator.onLine` in `SyncStatusBar`) — both avoid the hydration mismatch the effect was guarding against, without the lint violation.
  - "Prefill state once async data arrives" (vehicle info form prefilling from NHTSA decode): restructured so the form is a child component that only mounts once the data is already known (gated by a loading check in the parent), letting it use a lazy `useState(() => ...)` initializer instead of an effect + "have I prefilled yet" flag.
  - A synchronous one-time capability check (`use-camera.ts`'s "does `getUserMedia` exist") became a lazy initializer too; the actual async `getUserMedia().then()/.catch()` calls were already fine as-is — the rule only flags synchronous `setState` in the effect body, not calls inside async callbacks, which is the documented correct "subscribe, then update when the external thing resolves" shape.
- **CORS was not enabled on the backend** (`backend/src/main.ts`) until caught while smoke-testing the flow locally — the PWA frontend is always a different origin from this API (different port in dev, different subdomain in any real deployment), so every browser request would otherwise be silently blocked. Fixed via `app.enableCors()`, configurable through an optional `CORS_ORIGIN` env var (comma-separated allowlist; defaults to allow-all).

## Phase 4 — AI Orchestration Pipeline ✅ built and live-verified (2026-08-01, closed out 2026-08-02)
- [x] BullMQ + Redis wiring, concurrency capped to Gemini rate limit (`AI_QUEUE_CONCURRENCY`, conservative default of 2 — Gemini's exact rate limit for this project's plan tier isn't pinned down yet, see blocker below)
- [x] Gemini Vision integration w/ `response_mime_type: application/json` + Zod validation (`backend/src/ai/gemini.service.ts`, `gemini-response.schema.ts`) — no SDK dependency, plain REST
- [x] Idempotency key on AIAnalysis (`part_image_id` + `model_version`) — unique DB index from Phase 1 plus a pre-check in `AiAnalysisService` that skips a redundant Gemini call entirely on retry
- [x] Graceful degradation: exhausted retries → Part flips to `needs_manual_grading` (`AiAnalysisProcessor.onFailed`, only on the *last* attempt, not every transient one)
- [x] AIAnalysis + HumanCorrection persistence (`POST /ai-analyses/:id/corrections`, manager/owner only)
- [x] **Acceptance verified via tests:** job enqueue → AIAnalysis write (real BullMQ round trip in `parts.e2e-spec.ts`); retry does not duplicate (`ai-analysis.e2e-spec.ts`); malformed/schema-invalid Gemini JSON rejected, never stored (`gemini.service.spec.ts`, `ai-analysis.e2e-spec.ts`); simulated outage produces a manually-gradable Part (`ai-analysis.e2e-spec.ts`, `ai-analysis.processor.spec.ts`)
- [x] **Live Gemini call verified (2026-08-02).** See below — was blocked on zero quota all through the original build session, resolved once the user attached a billing account.

### Resolved: Gemini API quota was 0 on the connected Google Cloud project
`GEMINI_API_KEY` is saved in `backend/.env` (gitignored). Verified against the real API:
- The key authenticates correctly.
- The Gemini API had to be enabled on that project via the GCP console — done in the original session.
- **CLAUDE.md's "Gemini 3.5 Flash / 3.1 Flash-Lite" model names don't exist in the real API** as of the original session's check. As of this check (2026-08-02), Gemini has moved on twice more: `gemini-2.0-flash` (the model the original session pinned as the working default) and `gemini-2.5-flash` are now *both* dead with `404 "no longer available to new users"` — a real, recurring pattern, not a one-off. Code default changed from a pinned version to the Google-maintained alias `gemini-flash-latest` in `gemini.service.ts` and `ai-analysis.service.ts`, specifically to stop this from breaking the default again next time Google retires a version; `GEMINI_MODEL` still overrides to a pinned version for anyone who wants that instead.
- A real `generateContent` call against `gemini-flash-latest`, built with the exact request shape `GeminiService` sends (inline base64 image, `responseMimeType: application/json`, the real `GRADING_PROMPT`), returned `200` with `{"grade": "A", "damage_codes": [], "confidence": 0.9}` — parses correctly against `GeminiPartAnalysisSchema`. This is the first real end-to-end Gemini grading call this project has ever made; everything from auth through model resolution through response-schema validation is now confirmed working against the live API, not just mocked.
- Backend unit suite (15/15) and lint stay green after the default-model change; `gemini.service.spec.ts`'s tests are unaffected since they explicitly set `GEMINI_MODEL` in their own config rather than relying on the default.

### Test-infrastructure note: flaky e2e teardown (partially fixed, not fully eliminated)
Bootstrapping the full app (`AppModule`) in multiple e2e spec files, run sequentially in one Jest process (`--runInBand`, required since Phase 4 — see below), occasionally produced `Unhandled error: Connection is closed` crashes attributed to whichever spec file happened to be running when a stray async error fired — sometimes a completely unrelated file with no BullMQ code of its own (e.g. `rls-isolation.e2e-spec.ts`), confirming this is cross-file bleed, not a bug local to one file.

Root cause: BullMQ's `Worker` keeps a dedicated blocking Redis connection (for its internal blocking read) that doesn't necessarily finish unblocking by the time `app.close()`'s promise resolves; the connection can throw an unhandled `'error'` event asynchronously afterward, once a different spec file's app has already started booting.

Fixed, in order of what actually helped:
1. `GeminiService`'s `fetchImpl: typeof fetch = fetch` constructor parameter (a default-value pattern used throughout this codebase for test injection) broke Nest's DI once it was provided through a real module — Nest tries to resolve every constructor parameter as a token regardless of default values. Fixed with `@Optional()`. This alone was the cause of an early, worse failure mode (a misleading `DriverPackageNotInstalledError: pg` that was actually an orphaned retry loop from a *different* provider's partial DI failure).
2. Passing BullMQ's Redis connection as a pre-built `IORedis` instance meant BullMQ wouldn't close it on shutdown (it only closes connections it created itself) — every e2e run hung indefinitely after finishing. Fixed by passing connection *options* instead (`queues/redis-connection.ts`), letting BullMQ own the connection lifecycle.
3. `AiModule` redundantly called `BullModule.registerQueue()` even though `@Processor`/`WorkerHost` doesn't need it (it builds its own `Worker` from the root connection config) — this created a second, entirely unused `Queue` producer with its own connection and no error listener. Removed.
4. Added explicit `'error'` listeners on both the `Worker` (`AiAnalysisProcessor.onError`) and the one `Queue` actually in use (`PartsService`'s constructor) — EventEmitters with zero listeners for `'error'` crash the process, not just log a line; this is correct production hygiene regardless of the test flake.
5. `--runInBand` added to `test:e2e` (was already needed independently — running e2e spec files in parallel Jest workers meant every file's `AiAnalysisProcessor` competed for jobs on the same real Redis queue, so a job enqueued by `parts.e2e-spec.ts` could get grabbed by a different file's worker with a non-mocked `GeminiService`).
6. `test/close-test-app.ts`: every e2e spec's `afterAll` now explicitly awaits `processor.worker.close()` (graceful, no force flag) before `app.close()`, giving BullMQ's own shutdown sequence a chance to settle that blocking connection inside the owning file's teardown window. **Tried `close(true)` (force) first — under some timing it produced a native access violation (Windows exit code 3221226505) instead of just failing a test, i.e. forcing the socket closed mid-blocking-read is a real crash risk, not just noise. Switched to graceful `close()`.**

Net effect: failure rate dropped from roughly 1-in-2 runs to roughly 1-in-8 runs across ~15 repeated full-suite runs while diagnosing this. The residual flake looks like Windows-specific ioredis/BullMQ socket-teardown timing under rapid sequential Nest app boot/teardown — a pattern that only exists in this multi-file e2e run, never in real operation (a production app boots once and runs continuously). Stopped chasing it further past this point; if it becomes a real CI problem, the next lever to pull is investigating BullMQ's own internals for the blocking connection (blocked this session by a permission restriction on reading `node_modules` source directly) or splitting e2e specs that need a full app boot from those that don't (`rls-isolation` and `ai-analysis` already don't).

## Phase 5 — Desktop Manager Dashboard ✅ (2026-08-01)
- [x] Backend: Vehicles resource API (list/detail), Parts resource API (list/detail/approve), AI Review Queue API, Users management API (RBAC), Tenant Settings API (AI confidence threshold), CSV export endpoint, Analytics aggregation endpoint (`backend/src/{vehicles,parts,users,settings,analytics}/*`)
- [x] Desktop shell: `(desktop)/layout.tsx` manager/owner auth guard + `desktop-nav.tsx` (8 sections), same `restored`-flag pattern as the mobile layout
- [x] Global Dashboard (`(desktop)/dashboard`) — vehicle count, needs-review count, marketplace-ready count, each linking to its detail screen
- [x] AI Review Queue (`(desktop)/review-queue`) w/ keyboard nav (↑/↓ select, `a`/Enter approve), grade-override correction capture (records via `POST /corrections` before approving, only when the grade actually changed), visually distinct low-confidence/failed-analysis state
- [x] Inventory Management virtualized table (`(desktop)/inventory`) — `@tanstack/react-virtual`, fetches up to 1000 parts (backend's `ListPartsDto` cap) and renders only the visible window + overscan. Status filter dropdown. Functionally verified: a 10,000-item mocked dataset still renders <50 DOM row elements (`page.test.tsx`). **Playwright-trace gap closed (2026-08-01):** `frontend/e2e/inventory-virtualization.spec.ts` intercepts the real `GET /parts` network call with 1000 items (the real ceiling `ListPartsDto`'s pageSize cap allows, not an inflated fixture), navigates to `/inventory` as a seeded manager session, and sweeps a full scroll of the list. Row count stays bounded (22 before scroll, peaks at 32 during scroll, both ≪50/1000) and a `PerformanceObserver('longtask')` recorded just 1 long task (~105-110ms, the initial scroll trigger) across 20 scroll steps against a production build (`next build && next start` — dev-mode Turbopack overhead produced 7 long tasks including a 395ms outlier and isn't representative). Stable across repeated runs; real `trace.zip` saved as evidence.
- [x] Vehicles Management (`(desktop)/vehicles`) — list w/ VIN, description, crush status, parts count; crush-status filter. Not virtualized (vehicle counts are orders of magnitude smaller than parts).
- [x] Marketplace Syndication (`(desktop)/marketplace`) — marketplace-ready (approved+listed) count, CSV export button (blob download). CSV export only for MVP, per BUILD_PLAN Phase 6; direct eBay/Shopify/ACES integrations explicitly deferred.
- [x] Analytics (`(desktop)/analytics` + new `GET /analytics`) — total vehicles/parts, parts-by-status, completed-analysis grade distribution, vehicles-by-crush-status, all scoped to the caller's tenant (e2e-verified for cross-tenant isolation and RBAC denial, same as every other Phase 5 resource).
- [x] Users/RBAC (`(desktop)/users`) — add-user form (worker → PIN, manager → email+password, matching `CreateUserDto`'s own conditional shape), inline per-row role select (optimistic update + rollback on failure). Owner rows are read-only, mirroring the backend's rule that `Create/UpdateUserDto` never accept `UserRole.OWNER`.
- [x] Settings (`(desktop)/settings`) — AI confidence threshold, shown/edited as 0–100%, converted to the 0–1 fraction the backend stores and the Review Queue already reads for its low-confidence cutoff.
- [x] **Bug found + fixed during acceptance verification:** `CorrectionsService.recordCorrection()` only ever wrote the `HumanCorrection` log row — it never applied the corrected value onto the underlying `AiAnalysis`, so a manager's grade override in the Review Queue would approve a part whose Inventory listing and CSV export still showed the AI's original (possibly wrong) grade, silently failing the acceptance line below. Fixed: the corrected value is now applied onto `AiAnalysis` (grade/confidence/damage_codes) after the original value is logged for the Moat — regression-tested in `corrections.e2e-spec.ts`.
- [x] **Acceptance verified:** review → correct → approve → visible in Inventory → CSV export contains it, confirmed via the composition of `corrections.e2e-spec.ts` (correction applies to the analysis), `parts-listing.e2e-spec.ts` (`/parts/:id/approve` moves status, corrected grade shows in list/detail), and `parts-export.e2e-spec.ts` (approved parts + their current grade appear in the CSV); low-confidence items visually distinct (`review-queue/page.test.tsx`, `needsReview()`). Not re-verified live in a browser this session (Phase 3's precedent — user explicitly deferred live browser walkthroughs this session). Full backend suite: 15 unit + 58 e2e passing. Full frontend suite: 142 tests across 30 files passing. Both workspaces typecheck and lint clean (`tsc --noEmit`, `eslint --quiet`).

## Phase 6 — Integrations (MVP scope) ✅ (2026-08-01)
- [x] NHTSA error fallback to manual entry — already built in Phase 3 (`lib/nhtsa.ts`'s `VinDecodeError`, caught non-blockingly in `vin-page-client.tsx`, Vehicle screen renders blank editable fields when `draft.decoded` is null). Re-verified this session: `vin-page-client` test "still saves the VIN and navigates onward when NHTSA decode fails" covers both the network-failure and undecodable-VIN (HTTP 200, no make/model) cases.
- [x] Finalized CSV export format — already built in Phase 5 (`parts/csv.ts`, `GET /parts/export.csv`): `id,vin,title,description,grade,damage_codes,confidence,status,price` (price is an intentional MVP placeholder). `parts-export.e2e-spec.ts` verifies correct quoting of a comma-containing field and that only approved/listed parts are included.
- [x] eBay/Shopify/ACES direct integrations explicitly deferred (Marketplace screen states this) — not built, per BUILD_PLAN's own MVP scope.
- [x] **Acceptance verified:** VIN decode failure falls back gracefully to manual entry (test above); CSV opens correctly with AI-generated title/description/grade (`parts-export.e2e-spec.ts`).

## Phase 7 — Polish & Hardening ✅ (2026-08-01)
- [x] PWA manifest/service worker with CacheFirst/StaleWhileRevalidate strategies — `frontend/public/{manifest.webmanifest,sw.js,icons/*}`, registered from `(mobile)/layout.tsx` via `lib/pwa.ts`. Removed `@ducanh2912/next-pwa`: it generates its service worker via a `webpack()` config hook, but this project builds exclusively with Turbopack, which does not execute `webpack()` config or support webpack plugins at all — the dependency was dead weight, never actually producing a service worker. Replaced with a small hand-written `sw.js` implementing the two named strategies directly against the Cache API. Verified via a real `next build` (Turbopack) succeeding end-to-end and a live curl against the dev server.
- [x] **Bug found + fixed during this pass:** `frontend/src/app/page.tsx` was still the unmodified create-next-app scaffold from Phase 0, never replaced. Since `(mobile)/page.tsx` (route group) also resolves to `/`, and Turbopack silently let the root-level file win instead of erroring on the conflict, the real worker home screen (in-progress drafts, "New Vehicle") was completely unreachable this entire build. Deleted the scaffold file; confirmed via `next build` (no conflict) and a live curl (title/markup now match the real app).
- [x] Accessibility pass — audited every screen for label/input pairing, alt text, focus-visible states, semantic landmarks (`nav`/`main`), and color-redundant status text; all were already solid. Found and fixed one real gap: three desktop screens' error messages (Settings, Users, Marketplace) were missing `role="alert"`, present everywhere else — a screen reader would never announce those errors.
- [x] Loading/error/empty state audit (all fetches) — found a systemic gap: every Phase 5 desktop screen caught its initial fetch's error and silently substituted a default (0, an empty array, a fallback threshold), rendering identically to a legitimately empty tenant. Fixed across all 8 screens (Dashboard, Review Queue, Inventory, Vehicles, Marketplace, Analytics, Users, Settings): each now has a `role="alert"` error state with a Retry button, distinguishable from its empty state, regression-tested per screen.
- [x] Full regression pass, typecheck, lint — backend 15 unit + 58 e2e passing, `tsc --noEmit` and `eslint --quiet` clean, `nest build` succeeds. Frontend 152 tests across 31 files passing, `tsc --noEmit` and `eslint --quiet` clean (only the pre-existing, expected `react-hooks/incompatible-library` warning on Inventory's `useVirtualizer`), `next build` (Turbopack) succeeds.
- [x] No `any` types — grepped both workspaces' `src/` for `: any`, `<any>`, `as any`; zero matches in either.
- [x] **Acceptance verified:** a11y clean (fixed gap above), no `any` types, all state-completeness (loading/error/empty) checks pass. **Lighthouse PWA score gap resolved (2026-08-02):** turns out unproducable as literally worded — Lighthouse v10+ (this repo uses v13) removed the scored "pwa" category entirely; Google moved PWA/installability checks to Chrome DevTools' Application panel, which has no scriptable Lighthouse output (`node -e` against the installed package confirms zero `pwa`-related audits remain in `default-config.js`). Closed the underlying intent two ways instead: (1) `frontend/scripts/lighthouse-audit.mjs` runs Lighthouse's real remaining categories against the production build's `/login` page (reusing Playwright's already-downloaded Chromium via `chrome-launcher`) — **performance 77, accessibility 100, best-practices 100, seo 100**, report saved to `frontend/test-results/lighthouse-report.json`; (2) `frontend/e2e/pwa-installability.spec.ts` verifies the actual installability criteria that category used to score directly: manifest is linked/valid/all icons resolve, the service worker registers and takes control of the page (`clients.claim()`), `sw.js` itself serves real content. All 3 installability checks + the accessibility-100 score are real, run evidence for this line, not an assumption.

## Post-Phase-7 — Environment hardening (2026-08-01)

All 7 BUILD_PLAN phases are complete (above). After Phase 7 closed out, actually running both dev servers side-by-side (per the README's own documented setup steps) surfaced two more real bugs, not caught by any test suite since neither one is something a test suite would exercise:

- **Backend/frontend dev-server port collision.** `backend/src/main.ts` and the frontend's Next.js dev server both defaulted to port 3000. Running `npm run dev:backend` and `npm run dev:frontend` together — exactly as the README instructs — meant whichever started second crashed with `EADDRINUSE`. Fixed: backend now defaults to `PORT=3001` (`backend/src/main.ts`), and the frontend's `apiBaseUrl()` fallback (`frontend/src/lib/api.ts`) matches. Verified: both `npm run dev:backend` and `npm run dev:frontend`, started via their real README-documented commands with no manual `PORT` override, now boot cleanly at the same time; `curl localhost:3001/health` responds. README updated to state this as the actual default rather than a vague hedge.
- **Flaky-looking test suite failures were a real timeout budget problem, not flake.** Vitest's default 5s per-test timeout was too tight for `userEvent.type()` on longer strings (a 36-char tenant UUID, a 17-char VIN) once combined with this machine's jsdom environment-setup overhead under load — these tests were doing real, correct work, just not finishing within 5s, not hanging. Traced by comparing which tests failed (all did meaningful typing) against which passed (short strings or no typing) before concluding it wasn't app-logic flake. Fixed: `frontend/vitest.config.mts` now sets `testTimeout: 15000` globally, plus `userEvent.setup({ delay: null })` on the worst-offending device-setup test (removes an artificial per-keystroke pause the app's plain-onChange input never needed). Verified: full frontend suite green twice in a row after the fix (152/152), including a standalone rerun of the previously-failing files.

Both fixes committed together (backend/src/main.ts, frontend/src/lib/api.ts, frontend/vitest.config.mts, frontend/src/app/(mobile)/login/page.test.tsx, README.md).

## Notes
- Planning Gate review (docs/BUILD_PLAN.md § GSTACK REVIEW REPORT) completed 2026-07-31 — 11 findings, all fixed pre-build.
- Re-verify the RLS/connection-pooling leak risk (Phase 2, highest-severity finding) once auth middleware is implemented — easy to silently reintroduce on a pooling-strategy change.
- **Operational quirks found during Phase 0 (Windows + this npm version):**
  - `npm install <pkgs>` on an *existing* node_modules tree can crash with `TypeError: Invalid Version:` in npm's arborist dedupe logic when a package is both a direct and transitive dependency with different version ranges (hit with `bullmq`/`passport`). Fix: declare all deps directly in `package.json` and do one fresh `rm -rf node_modules package-lock.json && npm install` rather than incremental adds.
  - `typeorm@^1.1.0` peer-requires `ioredis@^5.x`; pinning `ioredis@^6` breaks `npm install` with a real ERESOLVE (not a bug — fix the version).
  - Piping `npm install ... | tail` (or similar) masks the real exit code in this shell — always check actual output/exit code directly (e.g. redirect to a file, `echo "EXIT=$?"` on its own line with no pipe) rather than trusting a green summary.
  - `npm run dev` (nest/next) invoked via the Windows `cmd.exe` wrapper loses buffered stdout when killed abruptly (e.g. by `timeout`); invoking the underlying binary directly (`node node_modules/next/dist/bin/next dev`, `node_modules/.bin/nest start`) gives reliable output for verification.
- **Recurring dev-mode nuisance (2026-08-02): the service worker reload-loop can come back even after the registration-timing fix.** The original bug (SW's `skipWaiting()`/`clients.claim()` racing the page's own load, see the CI section above) is fixed in code, but a *browser tab that already has an old SW installed from earlier testing* doesn't get that fix retroactively — it keeps running whatever `sw.js` it already installed. Hit this **three separate times** in one evening of active `frontend/src/**` editing under Turbopack HMR: a tab goes blank/stuck reload-looping again after a hot-reload, even with the fix in place. Recovery (same every time): navigate to a non-navigate-mode URL first to get a stable script-execution window (e.g. `/manifest.webmanifest` — a plain "/" navigation gets caught in the loop before any JS can run), then `(await navigator.serviceWorker.getRegistrations()).forEach(r => r.unregister())` + `(await caches.keys()).forEach(k => caches.delete(k))`, then reload. Worth a real fix if this becomes disruptive enough (e.g. a dev-only `sw.js` no-op, or skip SW registration entirely under `NODE_ENV=development`) rather than continuing to manually clear it.
